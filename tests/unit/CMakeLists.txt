# tests/unit/CMakeLists.txt
#
# Unit tests — fast, isolated, no I/O.
# Framework: Catch2 3 (found by parent tests/CMakeLists.txt).
# Location convention: tests/unit/{module}/{TASK-ID}-{slug}.cpp
#
# Populated by: AEV-001+ tasks.
# Currently empty.
