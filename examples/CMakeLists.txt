# examples/CMakeLists.txt
#
# Example applications demonstrating the Aevox public API.
# Built with EXCLUDE_FROM_ALL — not built by default, only when explicitly targeted.
# Location convention: examples/{example-name}/main.cpp
#
# Populated by: post-v0.1 tasks once the public API stabilises.
# Currently empty.
